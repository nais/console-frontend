environmentKinds:
  - management
values:
  host:
    displayName: override hostname during testing
    computed:
      template: '{{ subdomain . "console" }}'
    config:
      type: string
  tenantDomain:
    computed:
      template: '{{ subdomain . "" }}'
