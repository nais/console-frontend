query TeamRoles($team: Slug!) {
	team(slug: $team) {
		members {
			pageInfo {
				totalCount
			}
		}
		userIsOwner
		userIsMember
		userCanElevate
		deletionInProgress
		lastSuccessfulSync
		externalResources {
			gitHubTeam {
				slug
			}
		}
		purpose
	}
}
