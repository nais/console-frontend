query TeamCVEPage($identifier: String!) @cache(policy: CacheAndNetwork) {
	cve(identifier: $identifier) {
		id
		identifier
		title
		severity
		priority
		cvssScore
		epssScore
		epssPercentile
		hasKevEntry
		knownRansomwareUse
		detailsLink
	}
}
