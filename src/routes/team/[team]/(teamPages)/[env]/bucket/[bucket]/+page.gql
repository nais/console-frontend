query Bucket($env: String!, $team: Slug!, $name: String!) @loading(cascade: true) {
	team(slug: $team) {
		bucket(env: $env, name: $name) {
			id
			name
			env {
				name
			}
			workload {
				... on App {
					name
				}
				... on NaisJob {
					name
				}
			}
		}
	}
}
