query Secrets($team: Slug!) {
	team(slug: $team) {
		secrets(first: 20) @paginate(mode: SinglePage) {
			pageInfo {
				totalCount
				hasPreviousPage
				hasNextPage
				pageEnd
				pageStart
			}
			edges {
				node {
					name
					lastModifiedAt
					environment {
						name
					}
				}
			}
		}
		environments {
			name
		}
	}
}
