query Secrets (
	$team: Slug!
) @cache(policy: CacheAndNetwork)  {
	secrets(team: $team) @loading {
		env { name }
		secrets {
			apps @loading
			name
			data @loading {
				key
				value
			}
		}
	}
}
