query Workloads($team: String!) {
	team(name: $team) {
		name
		apps {
			edges {
				node {
					name
					image
					env {
						name
					}
					instances {
						name
						status
					}
					deploys {
						edges {
							node {
								created
							}
						}
					}
				}
			}
		}
	}
}
