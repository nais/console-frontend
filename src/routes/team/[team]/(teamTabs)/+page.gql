query Workloads($team: String!) {
	team(name: $team) {
		name
		apps(first: 2) @paginate(mode: SinglePage) {
			totalCount
			edges {
				node {
					name
					image
					...AppInstancesStatus
					env {
						name
					}
					deployed
				}
			}
		}
	}
}
