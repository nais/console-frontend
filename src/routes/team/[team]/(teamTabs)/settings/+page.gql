query TeamSettings($team: String!) @loading(cascade: true) @cache(policy: NetworkOnly) {
	team(name: $team) {
		id
		name
		deployKey {
			key
			created
			expires
		}
		slackAlertsChannels {
			env
			name
		}
		slackChannel
		description
	}
}
