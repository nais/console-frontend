query Members($team: Slug!) @loading(cascade: true) {
	team(slug: $team) {
		slug
		members(limit: 50) @paginate(mode: SinglePage) {
			pageInfo {
				totalCount
				hasNextPage
				hasPreviousPage
			}
			nodes @loading(count: 10) {
				user {
					email
					name
				}
				role
			}
		}
	}
}
