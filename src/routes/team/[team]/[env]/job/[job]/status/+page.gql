query JobStatusDetailed($app: String!, $team: String!, $env: String!) {
	naisjob(name: $app, team: $team, env: $env) @loading(cascade: true) {
		name @loading

		jobState @loading {
			state

			errors {
				...AppErrorFragment
			}
		}
		runs {
			name

			message
		}
		accessPolicy {
			inbound {
				rules {
					application
					namespace
					cluster
					mutual
				}
			}
			outbound {
				rules {
					application
					namespace
					cluster
					mutual
				}
			}
		}
	}
}
