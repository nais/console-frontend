query App($app: String!, $team: String!, $env: String!) {
	app(name: $app, team: $team, env: $env) @loading(cascade: true) {
		name
		ingresses
		deployInfo {
			timestamp
			deployer
			url
		}
		image
		...AppInstances
		...AppInstancesStatus
		...AccessPolicy
		...AutoScaling
		...Authz
		...Storage
	}
}
