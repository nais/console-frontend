query PostgresInstance($env: String!, $team: Slug!, $name: String!) {
	team(slug: $team) {
		slug
		environment(name: $env) {
			postgresInstance(name: $name) {
				id
				name
				teamEnvironment {
					environment {
						name
					}
				}
				audit {
					enabled
					url
					statementClasses
				}
				highAvailability
				state
				maintenanceWindow {
					day
					hour
				}
				majorVersion
				resources {
					cpu
					memory
					diskSize
				}
			}
		}
	}
}
