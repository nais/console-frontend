query InstanceGroupRedirect($app: String!, $team: Slug!, $env: String!)
	@cache(policy: NetworkOnly) {
	team(slug: $team) {
		environment(name: $env) {
			application(name: $app) {
				instanceGroups {
					name
					created
				}
			}
		}
	}
}
