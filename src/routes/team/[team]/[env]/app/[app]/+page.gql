query App($app: String!, $team: Slug!, $env: String!) {
	team(slug: $team) {
		environment(name: $env) {
			application(name: $app) {
				id
				name
				...AppInstances
				...Persistence
				...AppStatus
				...AppUtilization
				...Scaling
				...WorkloadImage
			}
		}
		viewerIsMember
		viewerIsOwner
	}
}
