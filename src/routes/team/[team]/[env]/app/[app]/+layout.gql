query AppNotificationState($app: String!, $team: String!, $env: String!) @loading(cascade: true) {
	app(name: $app, team: $team, env: $env) {
		appState {
			state
			errors {
				__typename
			}
		}
	}
}
