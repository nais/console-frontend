query AppLog($app: String!, $team: String!, $env: String!) @loading(cascade: true) {
	app(name: $app, team: $team, env: $env) {
		name
		instances {
			name
			log(tailLines: 1000) {
				time
				message
			}
		}
	}
}
