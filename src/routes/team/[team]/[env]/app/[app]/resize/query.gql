query ResizeApplicationData($team: Slug!, $env: String!, $app: String!) {
	team(slug: $team) {
		environment(name: $env) {
			application(name: $app) {
				resources {
					scaling {
						minInstances
						maxInstances
					}
				}
			}
		}
	}
}
