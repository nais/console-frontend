query IngressMetrics($team: Slug!, $app: String!, $env: String!) {
	team(slug: $team) {
		environment(name: $env) {
			application(name: $app) {
				...NetworkPolicy
				ingresses {
					url
					type
				}
			}
		}
	}
}
