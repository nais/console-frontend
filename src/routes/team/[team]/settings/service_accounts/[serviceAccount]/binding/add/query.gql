query AddBindingSALookup($team: Slug!) @cache(policy: CacheAndNetwork) {
	team(slug: $team) {
		serviceAccounts(first: 100) {
			nodes {
				id
				name
				workloadBindings(first: 100) {
					edges {
						node {
							workloadName
							environment
						}
					}
				}
			}
		}
	}
}
