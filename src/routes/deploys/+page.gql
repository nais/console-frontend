query Deploys {
	deployments(first: 10) @paginate(mode: SinglePage) @loading {
		totalCount @loading
		pageInfo @loading {
			hasNextPage
			hasPreviousPage
			startCursor
			endCursor
			from
			to
		}
		edges @loading(count: 10) {
			node {
				team {
					name
				}
				repository
				resources {
					group
					kind
					name
					version
					namespace
				}
				env
				statuses {
					status
					message
					created
				}
				created
			}
		}
	}
}
