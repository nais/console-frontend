query AdminUsers {
	users(first: 20) @paginate(mode: SinglePage) {
		edges {
			node {
				id
				name
				email
				externalID
				isAdmin
			}
		}
		pageInfo {
			hasNextPage
			hasPreviousPage
			pageStart
			pageEnd
			totalCount
			startCursor
			endCursor
		}
	}
}
