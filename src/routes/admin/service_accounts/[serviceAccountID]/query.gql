query AdminServiceAccountDetail($id: ID!) {
	serviceAccount(id: $id) {
		id
		name
		description
		createdAt
		updatedAt
		lastUsedAt
		workloadBindings(first: 20) {
			edges {
				node {
					teamSlug
					workloadName
					environment
				}
			}
		}
		...ServiceAccountRolesFragment
		...ServiceAccountAuthenticationFragment
		...ResourceActivityCardServiceAccountFragment
	}
	roles(first: 100, filter: { excludeGlobalRoles: false }) {
		edges {
			node {
				name
				description
			}
		}
	}
}
