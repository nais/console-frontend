query ReconcilerLogs {
	reconcilers {
		nodes {
			enabled
			displayName
			name
			description
			errors(first: 1000) {
				nodes {
					correlationID
					createdAt
					message
					team {
						slug
					}
				}
			}
		}
	}
}
